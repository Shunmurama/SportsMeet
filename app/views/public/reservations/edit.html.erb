<div class="container mt-5">
  <div class="text-center">
    <h4>予約編集</h4>
  </div>

  <% if flash[:alert] %>
    <div class="alert alert-danger">
      <%= flash[:alert] %>
    </div>
　<% end %>

      <div class="mt-4">
        <div class="alert alert-success">
            <%= @event.minimum_number %>人で予約してください。
        </div>
        <% if @event.available_numbers > 0 %>
          <div class="alert alert-success">
            予約可能な人数: <%= @event.available_numbers %>
          </div>
          <%= form_with model: @reservation, url: event_reservation_path, method: :patch do |f| %>
            <div class="form-group">
              <%= f.label :reserved_number, "予約人数" %>
              <div class="input-group">
                <%= f.number_field :reserved_number, class: "form-control" %>
                <div class="input-group-append">
                  <span class="input-group-text">人</span>
                </div>
              </div>
            </div>
            <%= f.hidden_field :event_id, value: @event.id %>
            <%= f.hidden_field :date, value: @event.date %>
            <%= f.submit "予約する", class: "btn btn-success btn-block" %>
          <% end %>
        <% else %>
          <div class="alert alert-danger">
            満員のため受付は終了しました。たくさんのご応募ありがとうございました。
          </div>
        <% end %>
      </div>

      <div class="mt-4">
        <%= link_to "戻る", event_reservation_path, class: "btn btn-secondary btn-block" %>
      </div>

</div>